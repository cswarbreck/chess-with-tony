<div class="notice-board">
    <div class="container" id="notice-header">
        <div class="row">

            <div class="col-sm-6">
                <h2>NOTICE BOARD</h2>
                <hr style="border-bottom: 3px double lightcoral; border-radius: 10px"></hr>
                <br>
                <p>To find out more about booking lessons for your child or for any other enquires, please fill out the contact
                    form below and I will make every effort to get back to you at the soonest possible time.
                    <br>
                </p>
                <p>If you can think of anything else to put here I can add it for you when we meet to discuss the site.
                </p>
            </div>
        </div>
    </div>
    <div class="container" id="notice-content">
        <div class="row">
            <div class="col-sm-7" id="notice-posts">
            </div>
            <div class="container">
                <h3 class="text-weight-normal">Showing {{ data.posts.results.length}} posts.</h3>
                <br>
                <hr style="border-bottom: 4px lightcoral double"></hr>
                <br> {{# each data.posts.results}}
                <div data-ks-editable="{{#if ../user}}{{adminEditableUrl ../../user _id}}{{/if}}" class="post">
                    <h2>
                        <a href="{{postUrl slug}}">{{{title}}}</a>
                    </h2>
                    <h5>Published {{_.publishedDate.format "D MMMM YYYY"}}</h5>
                    <p class="lead text-muted">{{{categoryList categories prefix="Posted in "}}} {{#if author.name.first}}by {{author.name.first}} {{/if}}

                    </p>
                    {{#if image}}
                    <img src="{{{cloudinaryUrl image width=160 height=160 crop='fit' }}}" class="img pull-right">{{/if}}
                    <p>{{{content.brief}}}</p>
                    {{#if content.extended}}
                    <p class="read-more">
                        <a href="{{postUrl slug}}">Read more...</a>
                    </p>{{/if}}
                    <hr style="border-bottom: 1px lightcoral double"></hr>
                </div>
                {{/each}}
            </div>
            <div class="col-sm-1">
            </div>

            <div class="col-sm-4" id="notice-categories">
                <div class="container" style="border: 1px double lightcoral; border-radius: 1px">
                    <br>
                    <h2>CATEGORIES</h2>
                    <div class="list-group text-center" style="font-size: 16px">

                        <a href="/blog" id="active" style="">All Categories</a>
                        <br style="border-bottom: solid 3px lightseagreen !important"> {{# each data.categories }}
                        <a href="{{categoryUrl key}}" class="false">{{name}}</a>
                        {{/each}}
                        <br>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{{>footer}}