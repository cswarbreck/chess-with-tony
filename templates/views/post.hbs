<div class="hero" style="background: url('https://images.pexels.com/photos/872959/pexels-photo-872959.jpeg?w=1260&h=750&dpr=2&auto=compress&cs=tinysrgb'); background-size: cover; background-position: center center">
    <div class="row">
        <div class="container title text-left" style="margin-top: 9rem; margin-bottom: 7rem">
        	<h1 class="text-light">{{data.post.title}}</h1>
        </div>
    </div>
</div>

{{!-- <div class="container">
	<div class="row">
		<div class="col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2">
			<article>
				
				<hr>
				<header>
					<h1>{{data.post.title}}</h1>
					<h5>
						{{{categoryList data.post.categories prefix="Posted in "}}}
						{{#if author.name.first}}by {{author.name.first}}{{/if}}
					</h5>
				</header>
				<div class="post">
					{{#if data.post.image}}<img src="{{{cloudinaryUrl data.post.image width=750 height=450 crop='fit' }}}" class="img-responsive">{{/if}}
					{{{data.post.content.extended}}}
				</div>
				<p><a href="/blog">&larr; back to the blog</a></p>
			</article>
		</div>
	</div>
</div> --}}

{{!-- <div class="container">
	<div class="row">
		<div class="col-sm-8 col-md-9">
			
				<hr>
				<header>
					<h5>
						{{{categoryList data.post.categories prefix="Posted in "}}}
						{{#if author.name.first}}by {{author.name.first}}{{/if}}
					</h5>
				</header>
				    <div class="post">
					
					{{{data.post.content.extended}}}
					</div>
						<p><a href="/blog">&larr; back to the blog</a></p>
					
					<div class="col-sm-4 col-md-3">
						<h2>Image Heading</h2>
					<div style="margin-top: 8rem;" class="">
						{{#if data.post.image}}<img src="{{{cloudinaryUrl data.post.image width=750 height=450 crop='fit' }}}" class="img-responsive">{{/if}}		
				</div>
			</div>
		</div>
	</div>
</div> --}}

    <div class="container" style="margin-bottom: 7.5rem; margin-top: 3.5rem; min-height: 800px">
      <div class="row">
        <div class="col-sm-8" style="margin: auto">
          <h2 class="title text-left">
			{{{categoryList data.post.categories prefix="Posted in "}}}
			{{#if author.name.first}}by {{author.name.first}}{{/if}}
		  </h2>
          <br></br>
            {{{data.post.content.extended}}}
			<br></br>
			<p><a href="/blog">&larr; back to events</a></p>
        </div>
        <div class="col-sm-4 text-left" style="margin: auto">
			{{#if data.post.image}}
			<h3>{{{data.post.title}}}</h3>
			{{/if}}
          <br></br>
		  {{#if data.post.image}}<img src="{{{cloudinaryUrl data.post.image width=750 height=450 crop='fit' }}}" class="card-img-top ">{{/if}}
        </div>
      </div>
    </div>

	{{>footer}}

{{!-- <div class="container" id="blogContent">
	<div class="row">
		<div class="col-sm-8 col-md-9" id="">
			<h3 class="text-weight-normal">Showing {{ data.posts.results.length}} posts.</h3>
			<div class="container">
				{{# each data.posts.results}}
				<div data-ks-editable="{{#if ../user}}{{adminEditableUrl ../../user _id}}{{/if}}" class="post">
					<h2><a href="{{postUrl slug}}">{{{title}}}</a></h2>
					<p class="lead text-muted">{{{categoryList categories prefix="Posted in "}}}
							{{#if author.name.first}}by {{author.name.first}}{{/if}}
					</p>
					{{#if image}}<img src="{{{cloudinaryUrl image width=160 height=160 crop='fit' }}}" class="img pull-right">{{/if}}
					<p>{{{content.brief}}}</p>
					{{#if content.extended}}<p class="read-more"><a href="{{postUrl slug}}">Read more...</a></p>{{/if}}
				</div>
				{{/each}}
			</div>
		</div>
		<div class="col-sm-4 col-md-3">
			<h2>Categories</h2>
			<div style="margin-top: 70px;" class="list-group">
				
				<a href="/blog" class="active list-group-item">All Categories</a>
				{{# each data.categories }}
				<a href="{{categoryUrl key}}" class="false list-group-item">{{name}}</a>
				{{/each}}
				
			</div>
		</div>
	</div>
	{{>pagination}}
</div>
 --}}